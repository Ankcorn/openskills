# TypeScript

This repo targets Cloudflare Workers and follows the TypeScript conventions in `spec.md`.

## Settings

- strict: true
- noUncheckedIndexedAccess: true
- ESM only (`"type": "module"`)

## Commands

- Typecheck: `npm run typecheck` (uses `tsgo` from `@typescript/native-preview`)
